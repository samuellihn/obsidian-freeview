{"pageProps":{"mdContent":"<p>We can split up our code into multiple files with header files.</p>\n<p>These can include <a href=\"functions-in-c\">functions</a> to modularize our code into different files</p>\n<p>Functions should be <strong>declared</strong> in header <code>.h</code> files and compiled in C <code>.c</code> files</p>\n<pre><code class=\"language-c\">//functions.h\nint add(int a, int b);\n</code></pre>\n<pre><code class=\"language-c\">//functions.c\nint add(int a, int b) {\n\treturn a + b;\n}\n</code></pre>\n<p>Then, we can include the header file in our main C file:</p>\n<pre><code class=\"language-c\">//main.c\n#include \"functions.h\"\n//rest of code .......\n</code></pre>\n<p>When compiling, we need to make sure to compile <strong>both</strong> C files</p>\n<pre><code class=\"language-sh\">gcc -pedantic -std=c99 -Wall -Wextra main.c functions.c\n</code></pre>\n<p>This will include and link both files</p>","pageTitle":"Multifile C Programs"},"__N_SSG":true}